{
  "name": "webhook-inspector-cameroon",
  "version": "1.0.0",
  "description": "🇨🇲 Application web moderne pour capturer, analyser et déboguer les webhooks de l'API Genuka dans le contexte camerounais",
  "module": "src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --outdir ./dist --target bun",
    "test": "bun test",
    "db:init": "bun src/database/init.ts"
  },
  "keywords": ["webhook", "cameroon", "genuka", "inspector", "debug", "api"],
  "author": "Genuka Team",
  "license": "MIT",
  "dependencies": {
    "uuid": "^9.0.0",
    "moment-timezone": "^0.5.43",
    "better-sqlite3": "^9.2.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/uuid": "^9.0.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
